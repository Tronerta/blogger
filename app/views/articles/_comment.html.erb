<div class="card mb-3">
	<div class="card-body">
		<h5 class="card-title"><%= comment.author_name %> says:</h5>
		<p class="card-text"><%= comment.body %></p>
		<small class="text-muted">Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</small>
	</div>
</div>